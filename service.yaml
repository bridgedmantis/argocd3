#service.yaml
apiVersion: v1
kind: Service
metadata:
  name: demo-service
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  type: ClusterIP
  selector:
    app: demo   # هر دو نسخه را شامل می‌شود
    version: green
  ports:
    - port: 80
      targetPort: 80
